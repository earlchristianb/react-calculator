/** @format */

import { BUTTON_ENUM } from "../../enum";

//to determine background color
export const OPERATIONS = [
	BUTTON_ENUM.ADD,
	BUTTON_ENUM.MULTIPLY,
	BUTTON_ENUM.SUBTRACT,
	BUTTON_ENUM.DIVIDE,
	BUTTON_ENUM.EQUALS,
];
